---
name: domain-expert-kb
description: 把一个技术方向的资料（本地文件 + 飞书云文档 + 联网理论 + 竞品专利）建成三层知识库，让 agent 成为该方向的专家助手，并据此产出 PPT、汇报材料、会议提纲、论文、专利交底书；同时提供学术论文与专利的检索能力。当用户要求 agent「学习某个技术方向的所有资料」「掌握我们的开发进度和思路」「作为某某领域的专家助手」，或要基于自己的项目资料写汇报、做PPT、列会议提纲、写论文、准备专利时使用。**也在用户要检索学术文献或专利时单独使用**——「查一下 XX 方向的论文」「搜搜这个主题有哪些专利」「现有技术检索」「看看这个方向有没有人做过」「帮我找几篇相关文献」「专利查新」，这类需求不必走完整建库流程，直接用本 skill 的检索脚本。也适用于用户说「把这些资料都学一遍」「结合我们的进展帮我写」「以我们项目为背景做一份材料」「帮我写专利交底」。核心是严格区分「我们项目的事实」「公开理论」「外部对标」三层来源并全程可溯源，方法论按弱模型也能稳定执行来设计，理论层与对标层可在联网强模型环境预先冻结成文件，供受限内网的离线弱模型消费。
---

# 领域专家助手：三层知识库与交付物生产

## 这个 skill 解决什么问题

"读一批文档然后回答问题"和"成为某个技术方向的专家助手"是两件事。后者要多做三件：

- **掌握项目的活状态**——不只是文档里写了什么，还有现在做到哪一步、当初为什么选这条路、哪些问题还悬着。文档是快照，项目是叙事。
- **补全公开理论**——你们的内部资料不会从头讲阿克曼转向几何，但写论文、给新人讲、跟供应商对线时都需要。
- **产出专业交付物**——PPT、汇报、会议提纲、论文、专利，每种的读者和规矩都不同。

而这三件事凑在一起会产生一个**新的、更危险的失误模式**：来源串味。把教科书理论当成团队成果去汇报，是尴尬；把公开的现有技术当成自己的发明写进专利，是法律风险。所以这套方法的骨架就是**三层知识严格分离**。

## 三层知识架构

| | 项目层 | 理论层 | 对标层 |
|---|---|---|---|
| 内容 | 你们的文档、飞书资料、数据、决策、进度 | 公开的学科理论与工程方法 | 竞品动向、学术文献、专利现有技术 |
| 来源 | 本地文件 + 飞书 | 联网检索 + 模型知识 | 联网检索 |
| 权威性 | 对"我们在做什么"是唯一权威 | 对"原理是什么"权威 | 对"别人做到哪"权威 |
| 变化速度 | 快，需持续同步 | 慢，可长期冻结 | 中等，定期刷新 |
| 保密性 | **机密** | 公开 | 公开 |
| 引用要求 | 必须给内部出处 | 标注公开来源，区分"已查证"与"仅模型知识" | 必须给可核查链接 |

**为什么这个划分值得付出额外的建库成本**：每一类交付物其实是这三层的不同配比。汇报＝项目层为主+对标层定位；论文＝理论层做背景+对标层做 related work+项目层做贡献；专利＝对标层做现有技术+项目层做技术方案。分层建好之后，写交付物变成了"按配方取料"，而不是每次从零组织。

还有个现实好处：**理论层和对标层可以在网络好、模型强的环境里先建好冻结成文件**，办公电脑上的弱模型直接消费成品。这正是让弱模型达到同等水平的关键——难的部分不在它身上发生。

## 目录结构

```
<知识库根目录>/
└── _kb/
    ├── PROFILE.md              # 专家人设：领域、项目背景、知识边界、说话方式
    ├── project/                # 项目层
    │   ├── INDEX.md            #   资料索引（本地 + 飞书）
    │   ├── TIMELINE.md         #   进度时间线
    │   ├── DECISIONS.md        #   决策记录：选了什么、否了什么、为什么
    │   ├── OPEN.md             #   待决问题与风险
    │   └── cards/              #   每份资料一张卡片
    ├── theory/                 # 理论层
    │   ├── MAP.md              #   学科地图
    │   ├── <主题>.md            #   每个理论主题一份
    │   └── SOURCES.md          #   来源清单与可信度
    ├── landscape/              # 对标层
    │   ├── COMPETITORS.md      #   谁在做，做到什么程度
    │   ├── PAPERS.md           #   关键文献
    │   └── PRIOR_ART.md        #   专利现有技术
    ├── blocks/                 # 素材块：交付物的原料（见下）
    └── _state.json             # 同步状态，支持增量更新
```

原始资料一律不动。飞书文档拉取为本地快照存进 `project/cards/`，这样办公电脑离线也能用。

## 素材块：让弱模型也能写出稳定质量的关键

这是整套方法里对弱模型帮助最大的机制，值得单独说。

**问题**：让弱模型"写一份汇报"，它要临场组织项目定位、技术路线、指标现状、难点、优势——每次组织的说法都不一样，质量随机，而且经常和上次写的材料自相矛盾。

**做法**：建库阶段把这些反复要用的论述**预先写好、经你确认、存成文件**，每块提供三种长度：

```
_kb/blocks/
  项目定位.md      # 一句话 / 一段 / 一页
  技术路线.md
  关键指标.md      # 现状 / 目标 / 对标
  核心难点.md
  差异化优势.md
  里程碑进度.md
  风险与待决.md
```

之后写任何交付物，都是**取块 + 按交付物调整框架**，而不是从零生成：PPT 封面用一句话版，正文用一段版，论文引言用一页版。

这带来三个收益：质量下限被你审过的文字锁住；所有材料口径一致；弱模型的任务从"创作"降级成"组装"——后者它做得好得多。

素材块的具体写法和三种长度的示例见 `references/build-playbook.md`。

## 阶段一：建库

六个步骤，详细操作见 `references/build-playbook.md`。**每步产出文件后停下**，让用户确认再继续——这既是质量关口，也让弱模型不必长流程连跑。

0. **勘察与人设** —— 摸清资料范围，写 `PROFILE.md` 定义专家身份和知识边界
1. **项目层·资料索引** —— 本地文件 + 飞书文档，逐份建卡
2. **项目层·进度与决策** —— 从资料和访谈里抽出 `TIMELINE.md`、`DECISIONS.md`、`OPEN.md`
3. **理论层** —— 从项目资料里反推需要哪些理论，联网补全，冻结成文件。**该建哪些学科模块**对照 `references/discipline-modules.md` 逐项过一遍，避免只建"最显眼的那个学科"
4. **对标层** —— 竞品、文献、专利现有技术。检索方法与自带脚本见 `references/literature-search.md`
5. **素材块** —— 综合前面四层，写出可复用的论述模块，交用户审定

第 2 步是让助手"懂项目"而不只是"读过项目文档"的关键，第 5 步是让弱模型产出稳定的关键。这两步最值得花时间。

## 阶段二：交付物生产

各类交付物的模板、配方和检查清单见 `references/deliverables.md`。通用四步：

1. **定规格** —— 交付物类型、读者是谁、篇幅、场合。读者不同内容差别极大，先问清楚再动手，别猜。
2. **配料** —— 按 `deliverables.md` 里对应类型的配方，从三层知识库和素材块取材
3. **组装** —— 套用对应模板
4. **过检查清单** —— 每类交付物有各自的检查项，其中**来源分层检查是所有类型的共同必检项**

## 单独使用：文献与专利检索

不必建整个知识库也能用这部分。用户说「查一下 XX 方向的论文」「这个主题有哪些专利」「做个现有技术检索」时，直接走 `references/literature-search.md`，用自带脚本：

```bash
python3 scripts/paper_search.py "主题关键词"          # arXiv + Crossref
python3 scripts/patent_search.py '"主题关键词"'        # Google Patents
```

三点要记住，展开见 `literature-search.md`：

- **和 agent-reach 分工**：通用网页、GitHub、社区讨论走 agent-reach；**学术库和专利库它不覆盖**，才用这里的脚本
- **网络路由**：脚本调的都是境外 API，受限内网不可用，那边改走国内入口（知网/万方/国知局），检索最好在可联网环境做完并把摘要全文冻结成文件带回去
- **专利检索的底线**：脚本产出是初筛线索不是检索报告，且**不填风险等级**——没依据的风险判断比留空危险。要主动告诉用户仍需专业机构出正式报告

## 引用纪律

这是整套方法不能妥协的底线，三层各有各的规矩：

- **项目层**：给出内部出处（文件名+位置，或飞书文档链接）。汇报里的每个数字都要能追回到源头。
- **理论层**：标注公开来源。同时区分**已联网查证**和**仅来自模型知识**——后者在冷门技术细节上出错概率不低，标出来让人知道哪些需要复核。
- **对标层**：必须有可点击的核查链接。专利现有技术尤其严格，没链接的不要写进 `PRIOR_ART.md`。

**三层内容在同一份交付物里出现时，必须让读者能分辨哪句话属于哪层。** 具体做法因交付物而异（论文靠引用格式，PPT 靠页脚标注，汇报靠段落分区），`deliverables.md` 里逐类说明。

## 维护

- 项目层需要定期同步（飞书文档会变、进度会推进），`_state.json` 记录各来源的同步时间和指纹，只处理变动的
- 决策记录是**只追加**的：方案变更时新增一条并标注"取代 D-00X"，不要改写历史。当初为什么选错，本身就是有价值的信息，写复盘和专利时都要用
- 理论层基本不用动，除非领域有重大进展
- 素材块在项目有重要进展后更新，更新后提醒用户重新审定

## 移植到办公电脑的弱模型环境

推荐分工：**建库在联网、强模型的环境做（理论层和对标层尤其），办公电脑只做同步项目层 + 生产交付物**。

具体到 Cline / Qwen Code 的配置方法，以及针对弱模型的分步驱动技巧，见 `references/porting-to-other-agents.md`。

## 参考文件与脚本

- `references/build-playbook.md` —— 建库六步详细操作、卡片模板、飞书接入、素材块写法
- `references/deliverables.md` —— PPT / 汇报 / 会议提纲 / 论文 / 专利 五类交付物的配方、模板与检查清单
- `references/discipline-modules.md` —— 机电一体化/汽车电子类项目的学科模块清单（M1-M8），建理论层时的检查表；**注意：只有模块定义，理论内容一律写进知识库的 `theory/`，不写进本 skill**
- `references/literature-search.md` —— 文献与专利检索：与 agent-reach 的分工、境内外后端路由、检索式设计、落库纪律
- `references/porting-to-other-agents.md` —— 移植到 Cline / Qwen Code，以及强弱模型分工方案
- `scripts/paper_search.py` —— 论文检索（arXiv + Crossref + 可选 Semantic Scholar），输出即 `PAPERS.md` 条目格式
- `scripts/patent_search.py` —— 专利检索（Google Patents），输出即 `PRIOR_ART.md` 条目格式，中英文检索式均可
